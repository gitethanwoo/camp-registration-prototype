# Program setup: programs, sessions, pricing, discount rules, waivers and the audit log

- Plan Type: ExecPlan
- Status: Completed
- Owner: Claude (slice 5 builder)
- Started: 2026-09-24
- Completed: 2026-09-24

> Maintain this file in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

Before registration opens, a WinShape administrator has to set up what families can buy: the program, its sessions and capacity pools, the price, deposit and payment plan, the cancellation and refund policy, discount rules and the waivers families sign. Today those values only exist in the seed, and the one setup page (`/admin/session`) can change a pool's capacity and nothing else. There is also no way to read the audit trail the rest of the app writes.

After this plan, Alex Morgan (the admin persona, `alex.morgan@winshape.example`) can sign in to the staff console and:

- K2: see every program by ministry with its type, forms, waivers and publish state; create a program as a Draft; submit it for approval; and see it go live on the guest site only after every step of the approval chain is approved by someone other than the submitter.
- K3: edit a session's name, dates, location, registration open and priority dates, and its capacity pools (add, rename, resize, remove an empty pool). Waitlist mode shows as locked to "Admin approval". Sessions at the retreat center show room capacity as read-only, "from Oracle Opera".
- K4: set price, deposit and the payment plan (installment count and final due date, with the computed schedule), and the cancellation and refund policy as a table of time windows. The staff cancel-and-refund screen (C3) reads that table. Changing a price never changes an existing order.
- K5: build discount rules (type, value, stacking, program or session scope, valid dates, usage cap) with a preview on a real registered camper, and a guard that refuses any rule that could take a camper's price below zero when stacked. Checkout and the guest quote honor the rule's scope, dates and cap.
- K7: see each waiver's versions with effective dates and acceptance counts per version, write a new draft version, submit it, and have a different admin approve it. Published versions are never edited in place; families who accepted an older version keep that record.
- K12: any staff member can read the audit log, filter it by event type, actor, dates and text, open an entry to see before and after values where they were recorded, and export the filtered list as CSV. Nothing can edit or delete an entry; the database refuses it.

To see it working: start the API and Vite (Concrete Steps), sign in as Alex, open "Programs" under Setup, open "Family Camp" (seeded Pending approval, step 1 already approved by Jamie Dalton), approve step 2, and see "Family Camp" appear at `/programs` on the guest site.

## Progress

- [x] (2026-09-24 22:40Z) Read AGENTS.md, QUALITY.md, PLANS.md, the master plan and the wave 1 integration plan, the K cards in `docs/product/screen-specs.md`, the concept PNGs for K2, K3, K4, K5, K7 and K12, and the code the slice reads (`Domain/Entities.cs`, `Features/Checkout.cs`, `Features/Pricing.cs`, `Features/AdminEndpoints.cs`, `Features/StaffCx/DiscountEndpoints.cs`, `web/src/pages/admin/SessionEditor.vue`).
- [x] (2026-09-24 23:55Z) API: entities, configurations, `Setup` migration (with the audit trigger), seed, endpoints for K2, K3, K4, K5, K7 and K12, with `api/Camp.Api.Tests/SetupTests.cs` (18 tests).
- [x] (2026-09-24 23:55Z) Shared reads: checkout and the guest quote apply discount rules; the C3 cancel quote reads the refund table.
- [x] (2026-09-25 01:10Z) Web: `ProgramList`/`ProgramSheet` (K2), `SessionSetup` (K3), `PricingPolicies` (K4), `DiscountRules`/`DiscountRuleSheet` (K5), `WaiverTemplates`/`WaiverHistory` (K7), `AuditLog` (K12), registered in `routes.ts` under Setup; `SessionEditor.vue` links admins to K3. Lint and typecheck clean.
- [x] (2026-09-25 02:10Z) e2e: `e2e/setup.spec.ts` (Family Camp approval, pool resize, new discount rule with preview, waiver v3 approval, audit log entry with changes, CET read-only check at desktop and phone).
- [x] (2026-09-25 03:00Z) Self-review (seven passes: correctness, authorization, data integrity, copy, phone layout, accessibility, scope) and desktop (1440×1000) and phone (390×844) screenshots of K2, K3, K4, K5, K7 and K12 against the concepts. Fixes: pool table and payment schedule no longer overflow at phone width; audit rows wrap on phone and show the area label; waiver text under review is readable (not greyed); alert titles wrap; grade 0 shows as K; host and partner codes show their source instead of repeating the code; backfilled approvals read "WIN (before migration)"; filtered loads ignore stale responses.
- [x] (2026-09-25 03:10Z) Gates green, plan completed.
- [x] (2026-09-24) Review fixes. Checkout, `register-context` and the guest quote refuse sessions of unpublished programs (Draft, Pending approval, returned or unpublished), so no path to families skips approval. Seeded discount uses now come from real orders (0 on a fresh database) instead of made-up 37/112/64. A used code's type and amount are locked with a 400; name, scope, dates, cap and stacking stay editable. K3's badge no longer shows "Opens soon" or "Priority registration", which checkout doesn't honour, and ON Session 3's seeded dates are in the past. Imported waiver versions and live templates dated after today read as live since the WIN import (Aug 1, 2026). Cheaper notes: audit rows name a discount scope in words; waiver draft saves are audited; a changed payment plan can't schedule an installment in the past; K5 says one code applies per order and locks the amount fields of a used code; K3's full-pool alert links to the waitlist. New tests: `A_program_takes_no_registrations_until_it_is_published`, `A_used_codes_type_and_amount_are_fixed_but_its_other_terms_stay_editable`, and a past-installment case in the pricing test.

## Surprises & Discoveries

- Observation: dev sign-in gives every admin the same email (`admin@winshape.example`), so two admins in the API tests look like one person to the separation-of-duties checks.
  Evidence: `ApiFactory.SignInAsStaff` builds the email from the role. The tests therefore approve rows the seed says someone else submitted, and check that the author is refused.
- Observation: the core seed's "now" is 2027–2028 (waivers effective Nov 2027), but the app uses the real clock. Family Camp's past session is dated Summer 2026 so it is in the past on the real clock.
  Evidence: `Data/Seed.cs` waiver `EffectiveDate = new(2027, 11, 1)`; `SetupResults.Today` is `DateTime.UtcNow`.
- Observation: the sidebar lists every route's `meta.nav` entry for every staff role, so CET and Finance will see the Setup entries even though the setup endpoints require the Admin policy.
  Evidence: `web/src/layouts/AdminLayout.vue` builds `groups` from `router.getRoutes()` with no role check, and `RouteMeta` in `web/src/lib/nav.ts` has no role field.

- Observation: the router already has a Setup entry "Sessions & capacity" (`/admin/session`, `SessionEditor.vue`), so a second "Sessions" entry for K3 read as a duplicate.
  Evidence: `web/src/router.ts` line 85. K3 has no sidebar entry; it is reached from a program's Sessions tab and from the link on `/admin/session`.

## Decision Log

- Decision: Program publish state lives in a slice entity (`ProgramSetup`) with a two-step approval chain (`ProgramApprovalStep`: Camp director, then Ministry owner). The final approval publishes the program by setting the existing `CampProgram.IsPublished`, which is what the guest site already filters on. The submitter can't approve, and one person can't approve both steps.
  Rationale: K2's trap is "publish with no approval state". Tying publish to the last approval means there is no path to the guest site that skips approval. Separation of duties is the point of an approval chain; with only one admin persona, the seed provides a program whose first step another person already approved, so the demo still works.
  Date/Author: 2026-09-24 / Claude
- Decision: Programs are editable only while Draft. Pending approval and Published programs are read-only; "Return to draft" (which unpublishes a published program) makes them editable again.
  Rationale: approvers must approve what they read, and a live program changing under families is exactly what the approval chain exists to stop.
  Date/Author: 2026-09-24 / Claude
- Decision: Waiver versions are a slice entity (`WaiverVersion`). The existing `WaiverTemplate` row keeps holding the live version (its `Version`, `Body` and `EffectiveDate`), because checkout requires a signature for every `WaiverTemplate` of a program. Approving a new version archives the old version row and copies the new text onto the template. Past `WaiverAcceptance` rows keep their `Version` snapshot, and the old text stays in `WaiverVersion`.
  Rationale: adding a row per version to `WaiverTemplates` would make checkout demand a signature on every old version. This keeps checkout untouched.
  Date/Author: 2026-09-24 / Claude
- Decision: Discount rules are a slice entity (`DiscountRule`) one-to-one with the existing `DiscountCode`. A rule created in K5 by an admin creates an approved code. Codes requested by hosts and partners (staff-cx `DiscountRequest`) show in K5 read-only with a link to the C8 queue, and their requested terms are not enforced by this slice.
  Rationale: the C8 queue stays the only way a host code goes live, and its toast "It works at checkout now" stays true.
  Date/Author: 2026-09-24 / Claude
- Decision: `Features/Checkout.cs` and `Features/GuestEndpoints.cs` call `DiscountRuleGate` so a code with a rule only applies inside its scope and dates and under its cap; checkout claims a use with a conditional update in its seat transaction and releases it when the card is declined. `GuestEndpoints.cs` is outside this slice's allowed list; it gets a one-line call.
  Rationale: a rule that checkout ignores is a stub. The quote and checkout must agree, or a family sees a discount at review that checkout then refuses.
  Date/Author: 2026-09-24 / Claude
- Decision: The cancellation and refund policy is a per-session table (`RefundTier`: days before start, refund percent, basis, admin fee). The seed writes the rules the C3 quote hard-coded (60+ days: 100% of amounts beyond the deposit; 14–59: 50%; under 14: none) for every existing session, and `AdminEndpoints.CancellationQuote` reads the table through `RefundPolicy`. `AdminEndpoints.cs` is outside the allowed list; the change is limited to that quote.
  Rationale: K4's trap is "the policy as free text", and the plan requires refunds to read it. Seeding the old rules keeps C3's numbers unchanged for other slices.
  Date/Author: 2026-09-24 / Claude
- Decision: The payment plan template is the existing `Session.PlanInstallments` plus `Session.BalanceDueDate`; the schedule is computed by the existing `Pricing.PlanSchedule`. No change to `Pricing.cs`.
  Rationale: checkout already builds installments from these two values, so the K4 preview is exactly what a family gets.
  Date/Author: 2026-09-24 / Claude
- Decision: The audit log is made immutable in the database with an `INSTEAD OF UPDATE, DELETE` trigger on `AuditEvents`, added in the `Setup` migration. Before and after values are stored in a slice entity (`AuditChange`) linked to the audit row the slice's endpoints record.
  Rationale: "no edit or delete" should hold even for code that forgets. No existing code updates or deletes audit rows (checked with grep). The wave integrator must carry the trigger SQL into the combined migration.
  Date/Author: 2026-09-24 / Claude
- Decision: K3 is a new page, `web/src/features/setup/SessionSetup.vue` (`/admin/setup/sessions` for the session picked in the scope bar, `/admin/setup/sessions/:id` for any session), instead of growing `SessionEditor.vue`. `SessionEditor.vue` (`/admin/session`, open to all staff) keeps its capacity editor and gets one change: admins see a link to session setup, and other staff are told only an admin can change session details.
  Rationale: `/admin/session` is used by CET and Finance and by other slices' specs; putting admin-only editing there would mix two permission levels on one page. The new page reads the setup endpoints, which are Admin-only.
  Date/Author: 2026-09-25 / Claude
- Decision: Setup pages show an "Administrators only" message when the API answers 403, because the sidebar shows Setup entries to every staff role (see Surprises). The audit log is readable by all staff and needs no such message.
  Rationale: the nav has no role filter and adding one is a shared-layout change outside this slice.
  Date/Author: 2026-09-25 / Claude

- Decision (review fixes, 2026-09-24): Checkout and the family `register-context` and quote endpoints require `CampProgram.IsPublished`, which only the last approval sets.
  Rationale: the guest program list already hid unpublished programs, but session ids are sequential, so a family could still buy a Draft program's session by id.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): Once a code has been used (rule uses or paid/pending orders), its type and amount can't change; the admin creates a new code. Scope, dates, cap, name and stacking stay editable.
  Rationale: staff-cx `TransferService.DiscountAfterMove` reprices a moved camper from the code's current terms, so editing a used code would change money on existing orders. This is the same rule as "never edit a published waiver version in place".
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): K3's status badge shows only what checkout does: "Not published", "Ended" or "Registration open". The open and priority dates are stored, shown in the form and labelled as not yet enforced. Seeded dates for sessions selling today are in the past.
  Rationale: a badge saying "Opens soon" next to 186 registrations and a working checkout contradicts itself. Enforcing the dates in checkout is a later wave's call.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): Seeded discount rules take `Uses` from orders that carry the code, so K5's "used N" ties to orders.
  Rationale: the dataset totals must tie out; no seeded order carries SIBLING10, EARLY50 or SPRING25.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): Only program fields lock while a program is Pending approval or Published. Session details, pools and pricing stay editable without another approval, and every change is audited with before and after values.
  Rationale: capacity and pricing work happens during registration (raising a pool, adding a session); sending it back through approval would take a live program off the guest site. Approval covers what the program is, not day-to-day capacity.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): Stacking is a setup-time guard only. Checkout and the quote accept one code per order, so K5 says so under the Stacking field.
  Rationale: multi-code checkout is outside this slice; the guard keeps future combinations under 100%.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): Approval steps are labelled by role ("Camp director", "Ministry owner"), but any admin can approve any step. Separation of duties is enforced by email (submitter and the other step's approver are refused), and dev sign-in gives every admin one email.
  Rationale: staff roles in WorkOS are `cet`, `finance`, `host` and `admin`; there are no director or ministry-owner roles to check. Real per-step roles need new WorkOS roles, which is a product decision.
  Date/Author: 2026-09-24 / Claude

## Verification

- 2026-09-24 (review fixes): `npm run verify:precommit` clean; `npm run test:api` 117 passed, 0 failed (20 in `SetupTests`); `E2E_BASE_URL=http://localhost:5185 npx playwright test e2e/setup.spec.ts e2e/smoke.spec.ts` on a freshly dropped `CampRegistration_Setup`: 12 passed. K3 (ON Session 3 reads "Registration open", 186 registered) and K5 (seeded uses 0 / cap) checked at 1440×1000 and 390×844.
- 2026-09-25 (final): `npm run verify:precommit` clean; `npm run test:api` 115 passed, 0 failed; `E2E_BASE_URL=http://localhost:5185 npx playwright test e2e/setup.spec.ts e2e/smoke.spec.ts` on a freshly dropped `CampRegistration_Setup`: 12 passed (6 setup + 4 smoke on desktop, 1 setup + 1 smoke on phone).

- 2026-09-25: `E2E_BASE_URL=http://localhost:5185 npx playwright test e2e/setup.spec.ts e2e/smoke.spec.ts` on a fresh `CampRegistration_Setup`: 12 passed.
- 2026-09-24: `npm run test:api` 115 passed, 0 failed (18 in `SetupTests`). `npm run check:api` clean.

## Outcomes & Retrospective

Delivered K2, K3, K4, K5, K7 and K12 for Alex. Program publish is gated by a two-step approval that the submitter can't complete; session setup edits dates, registration windows and pools with seat-safe rules; pricing writes the price, plan and a refund table that the C3 cancel quote reads; discount rules are enforced at checkout and the guest quote (scope, dates, cap), and the stacking guard is checked when a rule is saved (checkout takes one code per order); waiver versions need a second admin and keep past signatures on their version; the audit log is readable by all staff, filterable, exportable as formula-safe CSV, and immutable in the database.

Known gaps, left for later waves:
- Refund quotes read the session's current deposit and refund tiers, not those in force when the family bought. Existing registrations keep their price; a policy change applies to later cancellation quotes.
- K2 and K5 lists have no select column or sort indicators (Part 1 Pass 6 Data Tables).
- On a fresh database, K12 holds setup and seed rows only; refund, access-change and health-access events come from other slices' actions.
- Registration open and priority dates are stored and shown but not enforced at checkout.
- Host and partner request codes (C8) keep their requested terms unenforced; only admin rules are.
- No way to create a brand-new waiver template; K7 versions existing ones.
- The older Staff-policy `PUT /api/admin/pools` used by `/admin/session` still lets CET and Finance change capacity.
- The wave integrator must carry the `TR_AuditEvents_Immutable` trigger SQL into the combined migration.
- Setup datetimes are wall-clock (no time zone), like the rest of the app.
- The sidebar shows Setup entries to every staff role; non-admins get an "is for admins" message.

Lesson: the phone screenshots found every layout bug in this slice (tables with too many columns); check 390px before the first commit of a table.

## Context and Orientation

The repository is a WinShape camp registration prototype: an ASP.NET Core minimal API in `api/Camp.Api` (EF Core, SQL Server), Vue 3 with shadcn-vue components in `web/`, and Playwright specs in `e2e/`. `AGENTS.md` explains the "slice seams": an endpoint module is a class implementing `IEndpointModule` (found by assembly scan), entity mappings are `IEntityTypeConfiguration<T>` classes, demo data is an `ISeedModule` that runs after the core seed, staff mutations call `IAuditLog.Record` before `SaveChanges`, and web pages register through `web/src/features/<slice>/routes.ts` (the `adminRoutes` export, with `meta.nav` for a sidebar entry).

This slice owns `api/Camp.Api/Features/Setup/`, `web/src/features/setup/`, `api/Camp.Api.Tests/SetupTests.cs`, `e2e/setup.spec.ts`, and `web/src/pages/admin/SessionEditor.vue`. Money is stored in cents. A "capacity pool" is a slice of a session's seats (for example "Boys G6–8", 50 seats); `CapacityPool.Reserved` counts seats taken and only changes through a conditional SQL update so two people can't take the last seat. Policies (`api/Camp.Api/Auth/Identity.cs`): `Policies.Admin` is the admin role only; `Policies.Staff` is cet, finance or admin.

Shared files this plan edits, each recorded in the Decision Log: `api/Camp.Api/Features/Checkout.cs` and `api/Camp.Api/Features/GuestEndpoints.cs` (discount rules), `api/Camp.Api/Features/AdminEndpoints.cs` (refund table in the C3 quote), and `web/src/pages/admin/SessionEditor.vue` (a link to K3 for admins).

## Plan of Work

Milestone 1 is the API. In `api/Camp.Api/Features/Setup/` add the entities and their configurations (`ProgramSetup`, `ProgramApprovalStep`, `SessionSetup`, `RefundTier`, `DiscountRule`, `WaiverVersion`, `AuditChange`), a `SetupSeed` (Order 150) that backfills setup rows for existing programs, sessions, waivers and codes and adds the slice's own demo rows (a Family Camp program pending approval with a past Summer 2026 session whose families accepted waiver v2, a pending waiver v3, and two Day Camp discount rules), and one endpoint module per screen under `/api/admin/setup/...` (Admin policy) plus `/api/admin/audit-log` (Staff policy). Then add the `Setup` migration with the audit trigger and write `SetupTests.cs`.

Milestone 2 connects the shared reads: `DiscountRuleGate` in checkout and the guest quote, and `RefundPolicy` in the C3 quote.

Milestone 3 is the web: `ProgramList.vue` (K2, list plus sheet editor), `SessionSetup.vue` (K3), `PricingPolicies.vue` (K4), `DiscountRules.vue` (K5), `WaiverTemplates.vue` (K7) and `AuditLog.vue` (K12), registered in `routes.ts` under the Setup sidebar group.

Milestone 4 is `e2e/setup.spec.ts` and the self-review.

## Concrete Steps

From `/Users/ethanwoo/dev/camp-wt/setup`:

    scripts/dotnet.sh ef migrations add Setup --project api/Camp.Api --output-dir Data/Migrations
    ConnectionStrings__Default="Server=localhost,14333;Database=CampRegistration_Setup;User Id=sa;Password=Camp_Dev_Passw0rd!;TrustServerCertificate=True" WorkOS__RedirectUri=http://localhost:5185/api/auth/callback scripts/dotnet.sh run --project api/Camp.Api --urls http://localhost:5085
    API_URL=http://localhost:5085 npm --prefix web run dev -- --port 5185 --strictPort
    npm run test:api
    E2E_BASE_URL=http://localhost:5185 npx playwright test e2e/setup.spec.ts e2e/smoke.spec.ts

## Validation and Acceptance

- [x] `npm run verify:precommit` (pass)
- [x] `npm run test:api` (pass)
- [x] `E2E_BASE_URL=http://localhost:5185 npx playwright test e2e/setup.spec.ts e2e/smoke.spec.ts` on a fresh database (pass)
- [x] Desktop (1440×1000) and phone (390×844) screenshots of K2, K3, K4, K5, K7 and K12 compared with the concepts

## Idempotence and Recovery

The seed module checks for its own rows before inserting, so restarting the API is safe. To start over, drop the slice database (`docker exec camp-registration-prototype-db-1 /opt/mssql-tools18/bin/sqlcmd -C -S localhost -U sa -P 'Camp_Dev_Passw0rd!' -Q "DROP DATABASE [CampRegistration_Setup]"`) and start the API, which migrates and seeds.

## Artifacts and Notes

Screenshots were taken with a throwaway Playwright script against http://localhost:5185 (not committed): K2 list, sheet and approval tab; K3; K4; K5 list and sheet; K7; K12 list and entry, each at 1440×1000 and 390×844.

## Interfaces and Dependencies

New tables: `ProgramSetups`, `ProgramApprovalSteps`, `SessionSetups`, `RefundTiers`, `DiscountRules`, `WaiverVersions`, `AuditChanges`; trigger `TR_AuditEvents_Immutable`. New static helpers: `Camp.Api.Features.Setup.DiscountRuleGate` (`UsableAsync`, `ClaimAsync`, `ReleaseAsync`) and `Camp.Api.Features.Setup.RefundPolicy` (`TiersForAsync`, `Quote`).
