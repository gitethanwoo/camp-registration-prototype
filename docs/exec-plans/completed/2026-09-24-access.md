# Health settings and staff access: who can see health data, and who is on staff

- Plan Type: ExecPlan
- Status: Completed
- Owner: Claude (slice 10 builder)
- Started: 2026-09-24
- Completed: 2026-09-24

> Maintain this file in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

WinShape collects health information about campers. For Day Camp and Family Camp the family fills in a health form inside this platform during registration (allergies, medications, physician, insurance). For Overnight Camp the health profile lives in CampDoc, a separate system, and this platform only knows whether the family finished it. Today the platform stores the embedded health forms (the `HealthJson` column on `Registrations`) but nobody can read them, nobody can choose how a program collects health information except by editing the whole program, and nothing controls which staff may see those details. The staff roster itself exists only inside WorkOS (the identity provider; in production this is Microsoft Entra), so an admin cannot see who has access, what they can see, or when they last signed in.

After this plan, Alex Morgan (admin persona, `alex.morgan@winshape.example`) can:

- K11, Staff access (`/admin/setup/users`): see every staff member synced from the WorkOS organization `org_winshape_staff`, with their role, ministry scope, health-data access, last sign-in and status. People removed from the organization in WorkOS show as Revoked on the next sync, which runs automatically when the page opens and the last sync is more than 15 minutes old, or on "Sync now". Alex can change a person's ministry scope and health-data access; both changes are audited with before and after values. There is no password field, no password reset and no "add user" form: people are added and removed in the identity provider.
- K9, Health collection settings (`/admin/setup/health`): for each program, choose the collection mechanism (Embedded form, Third-party form with its link, or CampDoc), with a warning and an explicit confirmation when CampDoc is chosen for anything other than Overnight Camp, and choose which staff roles may view health details. The page lists the people who can actually view details right now.

And the settings are enforced: a staff member can open a camper's embedded health form (the "View health form" button on the admin registration detail page) only if they are active, have health-data access, hold a role the program allows, and their ministry scope covers the program. Every successful view writes a `health.viewed` audit row, and every refusal writes `health.view_denied`, both visible in the audit log (K12) and on the registration's Activity tab.

To see it working: start the API and Vite (Concrete Steps), sign in as Diane (CET), open a Day Camp registration and click "View health form": she is refused because she has no health-data access. Sign in as Alex, open Users, give Diane "Health details", open Health settings, allow the CET role on Day Camp · Atlanta and save. Diane tries again and sees the allergies and medications; the registration's Activity tab shows the view.

## Progress

- [x] (2026-09-24 11:05Z) Read AGENTS.md, QUALITY.md, PLANS.md, the master plan (Wave 3, slice 10), the wave 1 integration plan, the K9 and K11 cards and concepts, and the code this slice reads (`Auth/*.cs`, `Infrastructure/Modules.cs`, `Domain/Entities.cs`, `Features/AdminEndpoints.cs`, `Features/Setup/*`, `web/src/pages/admin/RegistrationDetail.vue`). Probed the WorkOS emulator's organization memberships API.
- [x] (2026-09-24 11:40Z) API: entities, configurations, `Access` migration, seed, K11 and K9 endpoints, health view endpoint, sign-in recording; `api/Camp.Api.Tests/AccessTests.cs` (19 tests in `AccessTests` and `AccessSyncTests`, all passing).
- [x] (2026-09-24 11:45Z) Web: `StaffUsers.vue` + `StaffSheet.vue` (K11), `HealthSettings.vue` (K9), `HealthRecordButton.vue` mounted on the admin registration detail page; lint and typecheck clean.
- [x] (2026-09-24 11:55Z) e2e: `e2e/access.spec.ts` (5 tests; with `smoke.spec.ts`, 10 passed on a fresh database).
- [x] (2026-09-24 12:05Z) Self-review (seven passes) with desktop (1440×1000) and phone (390×844) screenshots of K11, the K11 sheet (active and revoked), K9 (normal, CampDoc mismatch) and the health form dialog, compared with `K9-health-settings-*` and `K11-staff-roles-*`. Fixed: K9 table clipped its last column at 1440 and pushed the phone page to 518px wide (Location now shows at 2xl only, text cells wrap, grid children `min-w-0`); the Sync now button inherited the alert's muted color; phone K11 rows now carry the health-data badge; concurrent syncs could revoke and audit the same person twice (sync now runs one at a time, new test).
- [x] (2026-09-24 12:15Z) Gates green, plan completed.
- [x] (2026-09-24 14:30Z) Review fixes. Blocking: (1) the embedded-form backfill now takes allergies, dietary and ADA needs from the camper's profile (only medications, physician and insurance are made up), and `/api/admin/registrations/{id}` no longer returns the profile's allergies, dietary or ADA needs, so the Answers card no longer shows health details to every staff member; (2) the K11 ministry-scope help text and page description now say scope limits health details only; (3) the third-party form copy on K9 now says staff see the link, not families; (4) the program sheet shows the health method read-only on existing programs with a link to Health settings, `PUT /api/admin/setup/programs/{id}` refuses a method change, and `POST` refuses CampDoc outside Overnight Camp; (5) a staff cookie whose platform row is Revoked is rejected on the next request (`OnValidatePrincipal`). Non-blocking: the K11 filter reads "Unscoped (all ministries)"; K9's header reads "N of M complete in CampDoc" for CampDoc programs; the backfill covers every embedded-form program (Family Camp, Family Weekend, the retreats), not only Day Camp; first-sign-in staff rows get a `staff.added` audit row; K11 shows actor names without the role suffix. New API tests and one e2e assertion; see Verification.

## Surprises & Discoveries

- Observation: the WorkOS emulator lists organization memberships with the user embedded, so one call gives name, email, role and status for every staff member.
  Evidence: `GET http://localhost:4100/user_management/organization_memberships?organization_id=org_winshape_staff` with `Authorization: Bearer sk_test_default` returns `{"object":"list","data":[{"user_id":"user_01M…","role":{"slug":"admin"},"status":"active","user":{"email":"alex.morgan@winshape.example","first_name":"Alex",…}}, …]}`.
- Observation: the emulator is shared by every slice's worktree and by the main stack, so writing to it (changing a role, removing a membership) would change other slices' personas mid-run.
  Evidence: the environment notes for wave 3; e2e specs of other slices sign in as Diane (cet) and Marcus (finance).
- Observation: opening K11 right after sign-in started two syncs at once, and both revoked Morgan Ellis, writing two `staff.revoked` rows.
  Evidence: `StaffSyncRuns` held two rows 3 ms apart, each with `Revoked = 1`. After adding a one-at-a-time gate around reconcile-and-save, the same e2e run leaves one `staff.revoked` row and a second, empty run. `Syncs_started_together_revoke_and_audit_once` covers it.

## Decision Log

- Decision: Roles are read-only in this platform. The role column shows the WorkOS membership role; a role changed in WorkOS is picked up by the next sync and audited as `staff.role_synced` with before and after. Ministry scope and health-data access are this platform's own attributes and are edited on K11.
  Rationale: the identity provider is the source of truth for who is staff and in what role (FR-2, FR-6: "users synced from Entra"). Editing roles here would either drift from Entra or require writing to the shared emulator, which other slices depend on.
  Date/Author: 2026-09-24 / Claude
- Decision: A former staff member, Morgan Ellis (`morgan.ellis@winshape.example`), is seeded as an active platform user who is not in the WorkOS organization. The first sync marks them Revoked. Emulator personas are seeded by email so the page is populated before the first sign-in; sync and sign-in attach the WorkOS user id.
  Rationale: the card requires "deprovisioned users are shown as revoked automatically", and the demo needs a real row to show it without changing the shared emulator.
  Date/Author: 2026-09-24 / Claude
- Decision: Sync is triggered by opening K11 when the last sync is older than 15 minutes, and by "Sync now". No background job.
  Rationale: hosted services are registered in `Program.cs`, which this slice may not edit. Opening the page is when the answer matters, and "automatically on the next sync" still holds. A failed WorkOS call changes nothing and says so; a sync never revokes on an error.
  Date/Author: 2026-09-24 / Claude
- Decision: Health settings are per program, not per session. The K9 list shows each program with its sessions underneath.
  Rationale: checkout reads `CampProgram.HealthMechanism`, and this slice may not edit `Checkout.cs`. Every session of a program already shares the mechanism; a per-session override that checkout ignores would be a lie on screen.
  Date/Author: 2026-09-24 / Claude
- Decision: Health detail access is the conjunction of four checks, evaluated from the database on every request (not from the session cookie): the person's platform row is Active, their health-data access flag is on, their role is in the program's allowed roles, and their ministry scope is All or the program's ministry. CampDoc programs never return details (there are none in this platform; the response points to CampDoc).
  Rationale: the flag is a per-person clearance (K11), the role list is per-program policy (K9), and ministry scope is the PRD's "an admin scoped to one ministry sees only that ministry". Reading the database means a revocation or a flag change applies immediately, not after the 8-hour cookie expires.
  Date/Author: 2026-09-24 / Claude
- Decision: Shared edit to `Auth/AuthEndpoints.cs` (allowed by the slice section): sign-in upserts the staff member's platform row (last sign-in, WorkOS user id, name, role) and adds `camp:ministry_id` and `camp:health_access` claims; `/api/auth/me` returns them. Sign-in takes `TimeProvider` for the timestamp.
  Rationale: last sign-in must come from real sign-ins, and the plan asks for the flags on the session.
  Date/Author: 2026-09-24 / Claude
- Decision: Shared edit outside the slice section: `web/src/pages/admin/RegistrationDetail.vue` gets one import and one element that mount `features/access/HealthRecordButton.vue` next to "On file. Visible to camp health staff only."
  Rationale: health details are viewed from a registration; without an entry point the enforcement has nothing to enforce. The component, endpoint and copy live in this slice, so the shared edit is a single element. The forms slice (9) also edits this file; the insertion is in the Documents card, away from where answers are shown.
  Date/Author: 2026-09-24 / Claude
- Decision: `POST /api/access/staff/sync` reconciles under a process-wide `SemaphoreSlim`. Fetching from WorkOS stays outside the gate.
  Rationale: two tabs (or a double mount) must not audit one revocation twice. The prototype runs one API process; a multi-instance deployment would use a database lock instead.
  Date/Author: 2026-09-24 / Claude
- Decision: The seed backfills `HealthJson` for Day Camp registrations whose health form is Complete but empty (the core seed's filler campers), with plausible, deterministic answers. It changes no status, count or money.
  Rationale: the canonical dataset says Day Camp uses the embedded form, so a completed form has content; without it there is nothing to view in the demo. Only a null column is filled, so other slices' assertions are unaffected.
  Date/Author: 2026-09-24 / Claude

- Decision: (Review fixes) Shared edit to `Features/AdminEndpoints.cs`: the registration detail's `participant` no longer carries `dietary`, `allergies` or `adaNeeds`, and `web/src/pages/admin/RegistrationDetail.vue` drops the Allergies and Dietary rows from the Answers card (an empty card now says "No registration questions answered.").
  Rationale: those are health details (FR-112), and the card showed them to every staff member with no access check, a few pixels above a dialog refusing the same person the form. The only read of health details is now `GET /api/access/registrations/{id}/health`, which enforces K9/K11 and audits. Nothing else in the web app or e2e read those fields.
  Date/Author: 2026-09-24 / Claude
- Decision: (Review fixes) The seed backfill builds each form from the camper's profile (allergies, else "None known"; dietary, else "None"; ADA needs) and covers every embedded-form program, not just Day Camp. Campers with a peanut or bee-sting allergy get an EpiPen as their medication.
  Rationale: what the form says must agree with what the family entered (the Part 2 consistency rule), and a Complete embedded form should have content wherever the platform collects it.
  Date/Author: 2026-09-24 / Claude
- Decision: (Review fixes) Shared edit to `Features/Setup/ProgramSetupEndpoints.cs` and `web/src/features/setup/ProgramSheet.vue`: an existing program's health method is read-only on the program sheet, with a link to Setup › Health settings; the PUT refuses a changed method (400), and creating a program on CampDoc outside Overnight Camp is refused (400). `HealthAccessRules.IsOvernight(slug, name)` is the shared check.
  Rationale: K9's CampDoc guard, confirmation and `health.settings_changed` audit must not be bypassable from the next page. One place changes the method; the sheet points to it.
  Date/Author: 2026-09-24 / Claude
- Decision: (Review fixes) Shared edit to `Auth/AuthSetup.cs`: the cookie's `OnValidatePrincipal` calls `Features/Access/StaffSessionCheck.ValidateAsync`, which looks up the staff row by WorkOS user id or email and, when it is Revoked, rejects the principal and signs the cookie out.
  Rationale: Revoked must mean access removed across the console (refunds, registrations, households), not only for health reads. A staff member with no platform row is left alone (their sign-in creates one). Signing in again through WorkOS restores the row, as before. Cost: one indexed lookup per staff request.
  Date/Author: 2026-09-24 / Claude
- Decision: (Review fixes) Ministry scope stays a health-details scope. The K11 sheet says "Health details are limited to this ministry's programs. Registrations and households aren't scoped yet." and the page description says the same.
  Rationale: filtering the console's registration, search and waitlist queries by `camp:ministry_id` touches other slices' endpoints; the honest fix inside this slice is copy that matches the code, with the console-wide scope listed as a gap.
  Date/Author: 2026-09-24 / Claude
- Decision: (Review fixes) The third-party form link is shown to staff only. K9 says "Staff see this link on each registration's health form. Send it to families yourself; the family checklist doesn't show it yet."
  Rationale: nothing on the family side reads `ThirdPartyFormUrl` (the checklist lives in `FamilyReadModel.cs` and `GuestEndpoints.cs`, other slices' files). The copy now matches; the family-facing link is a gap.
  Date/Author: 2026-09-24 / Claude

## Verification

- (2026-09-24) `scripts/dotnet.sh test api/Camp.Api.Tests --filter FullyQualifiedName~Access`: 19 passed. `npm run check:api`: 0 errors.
- (2026-09-24) `npm run test:api`: 191 passed, 0 failed (20 in `AccessTests` and `AccessSyncTests`).
- (2026-09-24) Fresh `CampRegistration_Access`, API on 5090, Vite on 5190: `E2E_BASE_URL=http://localhost:5190 npx playwright test e2e/access.spec.ts e2e/smoke.spec.ts`: 10 passed.
- (2026-09-24) `npm run lint`, `npm run typecheck`: clean. Screenshots: no horizontal scroll at 390 (document scrollWidth 390 on K11 and K9).

- (2026-09-24, review fixes) `npm run verify:precommit`: pass. `npm run test:api`: 194 passed, 0 failed. Fresh `CampRegistration_Access`: `E2E_BASE_URL=http://localhost:5190 npx playwright test e2e/access.spec.ts e2e/smoke.spec.ts`: 10 passed. Browser at 1440 and 390: registration 259 has no Allergies row, and its health form reads "Allergies: None known" (profile empty); K9 Overnight Camp reads "167 of 186 complete in CampDoc"; the Family Camp program sheet shows the method read-only with the Health settings link; no horizontal scroll at 390.

## Outcomes & Retrospective

Shipped K11 (Setup › Users) and K9 (Setup › Health settings), plus "View health form" on the admin registration page, with access enforced server-side on every read and every attempt audited (`health.viewed`, `health.view_denied`). Staff and roles come from the WorkOS staff organization; ministry scope and health-data access are edited here and audited with before and after. Settings changes leave registrations, statuses and money untouched (tested).

Gaps: roles are read-only by design (change them in WorkOS). Health settings are per program, not per session, because checkout reads the program's mechanism. There is no background sync; opening K11 after 15 minutes, or Sync now, runs it. The CampDoc "integration status" in the K9 concept is not modeled; CampDoc programs show an info alert and a link instead. The registration page's activity list refreshes on reload, not the moment a health view is recorded. Ministry scope limits health details only: a scoped CET or Finance user still sees every ministry's registrations, households and waitlists, because the console's queries belong to other slices (the `camp:ministry_id` claim is on the session, ready for them). The third-party form link is shown to staff on the health form, not to families on their checklist. The `camp:ministry_id` and `camp:health_access` session claims refresh on the next sign-in; health reads and revocation read the database, so only `/api/auth/me` display hints lag.

## Context and Orientation

The repository is a .NET 10 minimal API (`api/Camp.Api`) over SQL Server with EF Core, and a Vue 3 web app (`web/`) built from shadcn-vue primitives in `web/src/components/ui`. Feature slices plug in through seams so they rarely edit shared files: an `IEndpointModule` class maps routes (found by assembly scan), an `IEntityTypeConfiguration<T>` maps an entity, an `ISeedModule` inserts demo rows at startup, and `web/src/features/<slice>/routes.ts` exports `adminRoutes` whose `meta.nav` adds a sidebar entry. All of this is described in `AGENTS.md` and `web/src/features/README.md`.

Identity: staff sign in through WorkOS AuthKit (locally the WorkOS emulator on port 4100, configured in `infra/workos/workos-emulate.config.yaml`). `api/Camp.Api/Auth/AuthEndpoints.cs` redeems the code, reads the organization and role, and issues the app's own cookie with a `camp:staff_role` claim. `Auth/Identity.cs` defines the policies (`Policies.Admin`, `Policies.Staff` and others) and the `StaffUser` record endpoints inject. Tests sign in with `factory.SignInAsStaff("admin")`, which posts to a Testing-only `/api/auth/dev-login` that goes through the same `SignInAsync`.

Health data today: `CampProgram.HealthMechanism` (`Embedded`, `ThirdParty`, `CampDoc`) in `Domain/Entities.cs`; `Registration.HealthStatus` (Complete, Incomplete, Missing, Not required) and `Registration.HealthJson`, the serialized `HealthForm` record from `Features/Checkout.cs` (Dietary, Allergies, AdaNeeds, Medications, PhysicianName, PhysicianPhone, InsuranceProvider). `Features/AdminEndpoints.cs` returns `HealthOnFile` but never the contents.

Audit: inject `IAuditLog` and call `Record(action, entityType, entityId, detail)` before `SaveChanges`. The setup slice's `SetupAudit.Record(audit, db, …, changes)` extension (in `Features/Setup/SetupShared.cs`) also writes before/after rows that the K12 audit log shows; this slice uses it.

Terms: "ministry scope" is the one ministry (for example WSC Camps) a staff member works in, or All ministries. "Health-data access" is a per-person flag: on means the person may view health details where the program allows their role; off means they see completion status only. "Sync" means reading the WorkOS organization's memberships and reconciling the platform's staff rows with them.

## Plan of Work

API, in `api/Camp.Api/Features/Access/`:

`AccessEntities.cs` defines `StaffMember` (WorkOS user id, email, first and last name, role, `MinistryId` null for all, `HealthAccess`, `Status` Active or Revoked, `LastSignInAt`, `SyncedAt`, `RevokedAt`, `CreatedAt`), `StaffSyncRun` (when, who, added, updated, revoked counts) and `ProgramHealthSetting` (program id, allowed viewer roles as a comma list, third-party form URL, updated at and by), with their `IEntityTypeConfiguration`s (unique email, unique program id).

`StaffDirectory.cs` reads the WorkOS memberships through a named `HttpClient` ("workos-directory") created from `IHttpClientFactory`, following `list_metadata.after` for paging. `StaffSync.cs` reconciles: add new members, update names and roles (auditing role changes), restore a revoked member who is back, revoke active rows that are no longer members. `StaffAccessRules.cs` holds the four-check health rule and the effective viewer list.

`AccessEndpoints.cs` maps, all under `/api/access` and `Policies.Admin` unless noted: `GET staff`, `POST staff/sync`, `GET staff/{id}`, `PUT staff/{id}` (ministry scope and health access; 409 when revoked); `GET health`, `PUT health/{programId}`; and `GET registrations/{id}/health` under `Policies.Staff`.

`AccessSeed.cs` (Order 950) seeds staff rows for the emulator personas and Morgan Ellis, a health setting per program (admin only for embedded and third-party programs, none for CampDoc), and the Day Camp `HealthJson` backfill.

Shared: `Auth/AuthEndpoints.cs` upserts the staff row on sign-in and adds the claims.

Web, in `web/src/features/access/`: `routes.ts` (Setup › Users and Setup › Health settings, admin only), `StaffUsers.vue` and `StaffSheet.vue`, `HealthSettings.vue`, `HealthRecordButton.vue`, `types.ts`.

## Concrete Steps

From `/Users/ethanwoo/dev/camp-wt/access`:

    scripts/dotnet.sh ef migrations add Access --project api/Camp.Api --output-dir Data/Migrations
    ConnectionStrings__Default="Server=localhost,14333;Database=CampRegistration_Access;User Id=sa;Password=Camp_Dev_Passw0rd!;TrustServerCertificate=True" WorkOS__RedirectUri=http://localhost:5190/api/auth/callback scripts/dotnet.sh run --project api/Camp.Api --urls http://localhost:5090
    API_URL=http://localhost:5090 npm --prefix web run dev -- --port 5190 --strictPort
    npm run test:api
    E2E_BASE_URL=http://localhost:5190 npx playwright test e2e/access.spec.ts e2e/smoke.spec.ts

## Validation and Acceptance

- [x] `npm run verify:precommit` (pass)
- [x] `npm run test:api` (pass, including `AccessTests`)
- [x] `E2E_BASE_URL=http://localhost:5190 npx playwright test e2e/access.spec.ts e2e/smoke.spec.ts` (pass)
- [x] Desktop (1440×1000) and phone (390×844) screenshots of K11, K9 and the health form dialog compared with the concepts

## Idempotence and Recovery

The seed checks for its own rows (by email, by program id, and only fills a null `HealthJson`), so restarting the API is safe. Sync is idempotent: running it twice with no WorkOS change adds, updates and revokes nothing. To reset the slice database, drop `CampRegistration_Access` (see the environment notes in the master plan) and restart the API.

## Artifacts and Notes

None yet.

## Interfaces and Dependencies

No new packages. `IHttpClientFactory` (registered by `AddHttpClient` in `AuthSetup`) and `IOptions<WorkOsOptions>` supply the WorkOS base URL, API key and staff organization id. `TimeProvider` supplies "now".
