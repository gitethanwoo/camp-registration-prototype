# Groups and cohorts: a leader registers a group, attendees finish their own forms by secure link

- Plan Type: ExecPlan
- Status: Completed
- Owner: Claude (slice 3 builder)
- Started: 2026-09-24
- Completed: 2026-09-24 (review fixes 2026-09-25)

> Maintain this file in accordance with `docs/PLANS.md`.

## Purpose / Big Picture

WinShape Leadership (WSL) runs the Emerging Leaders Cohort, a program that a group leader registers on behalf of a team. Today the prototype shows the cohort on the Programs page but says "This flow isn't part of the prototype yet." After this work, a group leader (the seeded persona Pastor Dave, `pastor.dave@example.com`) can do three things end to end on real database rows.

First, on screen R8 ("group roster entry") the leader opens the cohort, chooses "Register your group", and types or pastes the attendees. Only a name is needed per row; an email is asked for but a row may be left partial. The leader pays for the whole group once with the simulated Fiserv card fields. Seats come out of the cohort's capacity pool atomically, so a group can never overbook the session.

Second, every attendee with an email gets a secure link (screen G1). The link opens a page that needs no account. It says who registered them, shows only their own information, and asks for a contact email, the program questions, and the versioned waivers. Submitting marks that attendee's forms Complete. The same page lets an attendee ask to withdraw.

Third, the leader's tracker (screen G2) shows how many attendees are Complete and Incomplete, lets the leader resend links (one or many), add a missing email, and approve or decline a withdrawal request. Approving refunds that attendee's price to the leader's card and releases the seat. The tracker states plainly that the leader can't complete forms on an attendee's behalf, and there is no edit-forms button.

To see it working: sign in as Pastor Dave, open "Emerging Leaders Cohort" from Programs, register a group, pay with 4242 4242 4242 4242, land on the tracker, press "Resend link" on an attendee, open the link shown in the dialog in a private window, submit the forms, and watch the tracker count move from Incomplete to Complete. The seeded group "Northside Fellowship leaders" shows the canonical numbers: 14 attendees, 9 Complete, 5 Incomplete, and one pending withdrawal request.

## Progress

- [x] (2026-09-24 14:00Z) Read AGENTS.md, QUALITY.md, PLANS.md, the wave plan, the screen cards R8/G1/G2 and the concept PNGs; wrote this plan.
- [x] (2026-09-24 15:10Z) API: entities, EF configuration, seed module, `Groups` migration, endpoints in `api/Camp.Api/Features/Groups/`.
- [x] (2026-09-24 15:25Z) API integration tests in `api/Camp.Api.Tests/GroupsTests.cs`: 11 pass.
- [x] (2026-09-24 17:20Z) Web pages: My groups list, R8 roster + pay, G2 tracker, G1 secure link; Cohort button on the program page.
- [x] (2026-09-24 17:50Z) Playwright spec `e2e/groups.spec.ts`: 2 tests (the seeded one also runs at phone width), passing with smoke.
- [x] (2026-09-24 18:30Z) Seven-pass self-review with desktop and phone screenshots; fixes applied (see Artifacts and Notes).
- [x] (2026-09-24 18:45Z) All gates green; plan completed.
- [x] (2026-09-25 01:30Z) Review fixes: atomic withdrawal-approval claim (no double refund or double seat release), interrupted group payments no longer strand the group (charge and step 3 ignore client aborts; `ReconcileAsync` heals a Draft group whose order was settled by the shared reconciler), R8 sideways scroll removed (hidden CSV input), plus copy fixes and Pending-replay handling in R8. 4 new API tests, 1 new e2e test; all gates green.

## Surprises & Discoveries

- Observation: a shadcn `AlertDialogAction` closes the dialog before its click handler runs, so a confirm handler that reads the dialog's target from state found it already cleared and did nothing.
  Evidence: the first e2e run showed the refund dialog close with no request sent. The tracker's confirm button is a plain `Button` inside `AlertDialogFooter`, which also keeps the dialog open with a spinner while the refund runs.

- Observation: the core seed already sets the cohort pool's `Reserved` to 14 (Carmen Ortiz's 14 attendees in the canonical dataset) without any rows behind it.
  Evidence: `api/Camp.Api/Data/Seed.cs`, `cohortS.Pools.Add(new CapacityPool { Name = "Attendees", ..., Capacity = 60, Reserved = 14 })`. The groups seed module adds exactly 14 attendee rows so the number is backed by data, and does not reserve seats again.
- Observation: the cohort program has no waivers or questions in the core seed, so an attendee link would have nothing to complete.
  Evidence: `Seed.cs` adds `Waivers` only to Overnight Camp and Day Camp. The groups seed module adds two waivers and four questions to the cohort program when they are missing.

## Decision Log

- Decision: The seeded group leader is Pastor Dave (Dave Kim), not Carmen Ortiz.
  Rationale: the canonical dataset names Carmen Ortiz as the WSL group leader, but the WorkOS emulator only has Pastor Dave as a group-leader persona (`infra/workos/workos-emulate.config.yaml`). A seeded tracker nobody can sign in to would be undemonstrable. The group keeps every canonical number (14 attendees, 9 Complete, 5 Incomplete, $450 each, September 2028).
  Date/Author: 2026-09-24 / Claude
- Decision: Group attendees are their own entity (`GroupAttendee`) rather than `Person` plus `Registration` rows.
  Rationale: a `Registration` requires a `Person` in a `Household`, and an attendee has neither; they may never create an account (FR-34). Inventing a household per attendee would pollute family search and the admin registration counts that other slices own. The cohort pool's `Reserved` still counts group seats, so capacity stays one number.
  Date/Author: 2026-09-24 / Claude
- Decision: The group is paid in full by the leader through a `PaymentOrder` with no `Registration` rows, charged by the slice-owned `GroupService` (`GroupService.CheckoutAsync`). `Checkout.cs` is untouched.
  Rationale: the cohort has no deposit or plan (Part 3 lists $450 per attendee). Reusing `PaymentOrder` and `PaymentOperation` keeps charges and refunds in the same ledger finance reads. Seats are claimed with one conditional UPDATE (`Reserved + n <= Capacity`) before charging; a decline releases them and returns the group to Draft with the roster intact.
  Date/Author: 2026-09-24 / Claude
- Decision: Secure-link tokens are 32 random bytes, stored only as a SHA-256 hash. "Resend link" rotates the token, and the old link stops working.
  Rationale: a leaked database row can't be turned into a working link. The emailed link has to travel in the outbox payload for HubSpot to send it; the demo also shows the new link in a dialog because there is no real inbox (wave plan: "show the link in the UI for the demo").
  Date/Author: 2026-09-24 / Claude
- Decision: R8's stepper has three steps (Program, Attendees, Review & pay). The concept's separate "Forms" step is dropped.
  Rationale: the leader never completes attendee forms (G2 card: "you can't complete forms on their behalf"), so a leader-facing Forms step has nothing to do.
  Date/Author: 2026-09-24 / Claude
- Decision: Shared edit to `web/src/pages/guest/ProgramDetail.vue`: for Cohort programs, add a "Register your group" button per session and drop the "isn't part of the prototype yet" sentence from the Cohort alert (the alert's explanation stays).
  Rationale: without it, R8 is only reachable by typing a URL. The edit touches only the Cohort branch of the non-standard alert; slice 2 may touch the Admittance branch of the same block, so the integrator may see a small adjacent-line conflict.
  Date/Author: 2026-09-24 / Claude
- Decision (review fixes): approving a withdrawal first claims the request with a conditional UPDATE (`Requested → Refunding`, new `WithdrawalStatus` value; enums are stored as strings, so no migration). Only the claim winner calls the processor and releases the seat; a failed or thrown refund reverts the claim. Decline is conditional the same way, so it can't race an approve.
  Rationale: the reviewer reproduced two parallel approves refunding $900 for a $450 attendee and freeing two seats. If the ledger save fails after a successful refund, the attendee stays in `Refunding` and can't be re-claimed, so a retry can never refund twice; staff finish it from the processor record. The processor's `RefundAsync` takes no idempotency key and `PaymentGateway.cs` is shared, so the claim is the guard.
  Date/Author: 2026-09-25 / Claude
- Decision (review fixes): group checkout passes `CancellationToken.None` to the charge and step 3, and step 3 finalizes only if the order is still Pending. `GroupService.ReconcileAsync` runs at the start of checkout and on `GET /api/groups/{id}`: a Draft group holding an order that is no longer Pending is confirmed with links sent (order Paid) or has its seats released and roster unlocked (order Declined), with an audit row either way. A conditional UPDATE on the group picks one winner, so parallel reads heal once. `Workers.cs` is untouched.
  Rationale: a leader closing the tab during "Processing…" aborted step 3; the shared `PendingPaymentReconciler` then settled the order through `CheckoutService`, which only knows Registration rows, leaving the group Draft and locked for good. For the integrator: the reconciler still writes the Charge operation and order status for group orders, which is what `ReconcileAsync` relies on; it doesn't need to skip them. If the process dies before the reconciler runs, the group is healed the next time the leader opens it after the reconciler pass.
  Date/Author: 2026-09-25 / Claude
- Decision (review fixes): the R8 CSV file input is `class="hidden"` instead of `sr-only`; the "Upload CSV" button still calls `.click()` on it.
  Rationale: the `sr-only` input sat outside the viewport and made the page scroll sideways (scrollWidth 1599 at 1440, 405 at 390). It is `aria-hidden` and out of the tab order anyway, so hiding it loses nothing.
  Date/Author: 2026-09-25 / Claude
- Decision: G1 renders inside the guest layout (logo, top nav) instead of a bare secure-link shell.
  Rationale: slice routes can only be children of `GuestLayout` and this slice may not edit shared files. The page itself shows no account, no other attendees, and states "You don't need an account." A bare shell needs a small `meta` hook in `GuestLayout.vue`, recorded here for wave 2.
  Date/Author: 2026-09-24 / Claude

## Verification

- (2026-09-24) `scripts/dotnet.sh test api/Camp.Api.Tests --filter FullyQualifiedName~GroupsTests`: Passed 11, Failed 0.
- (2026-09-24) `npm run test:api`: Passed 27, Failed 0.
- (2026-09-24) `E2E_BASE_URL=http://localhost:5183 npx playwright test e2e/groups.spec.ts e2e/smoke.spec.ts`: 8 passed (2 groups tests on desktop, the seeded one again on phone, 5 smoke).
- (2026-09-24) `npm run lint`, `npm run typecheck`, `npm run check:api`: pass.
- (2026-09-25, review fixes) The 4 new `GroupsTests` cases (parallel approvals, tab closed mid-charge, stranded group healing paid and declined) fail on the previous commit and pass now. Live check on a fresh `CampRegistration_Groups`: three parallel approves for Mateo Silva returned 204, 400, 400; refunded $450; seats left 44 → 45. R8 `scrollWidth`/`clientWidth`: 1440/1440 and 390/390.
- (2026-09-25, review fixes) `npm run verify:precommit`: pass. `npm run test:api`: Passed 31, Failed 0 (15 in `GroupsTests`). `E2E_BASE_URL=http://localhost:5183 npx playwright test e2e/groups.spec.ts e2e/smoke.spec.ts` on a fresh database: 10 passed.

## Outcomes & Retrospective

Delivered R8, G1 and G2 end to end on real rows, plus a "Your groups" list and the Cohort entry button on the program page. A leader registers a roster (typed, pasted, or CSV), pays once, and attendees finish their own forms by a rotating secure link; the leader resends links, adds missing emails, and approves or declines withdrawals, with refunds and seat release in the same ledger as family checkout.

Known gaps, left for later waves:
- `PendingPaymentReconciler` (Workers.cs) settles a stranded group order's status and charge row but knows nothing about groups; the group itself is healed by `GroupService.ReconcileAsync` the next time the leader opens it or retries checkout. Until then the group shows Draft with seats held. A background pass for groups would need a hook in shared `Workers.cs`.
- Integrator notes from review: the shared edit to `ProgramDetail.vue` needs explicit acceptance (expect a conflict with the admittance slice in the same alert block); nothing shared links to `/groups` yet, so a "Your groups" item in the `GuestLayout` user menu is needed; a Carmen Ortiz persona in the WorkOS emulator config would let the seed match the canonical dataset; admin session stats count group seats as "Held" because group attendees aren't Registration rows.
- Demo-only: the leader sees working secure links in the resend and add-email dialogs (wave plan: show the link in the UI). Remove before a real HubSpot integration. Secure-link submissions and withdrawal requests are audited with actor "System" (anonymous caller); the attendee name is in the detail.
- G1 renders inside the guest layout (header with Sign in), not a bare secure-link shell; needs a `meta` hook in `GuestLayout.vue`.
- No waitlist for groups: a roster larger than the remaining seats is refused with a clear message.
- The outbox dispatcher logs each payload, including the secure link, at Information level. That is shared code; the link should be redacted before a real HubSpot integration.
- Each e2e run nets two cohort seats (3 claimed, 1 released); after about 20 runs on one database the roster test needs a database reset.

## Context and Orientation

The repository is a .NET 10 minimal API (`api/Camp.Api`) over SQL Server with EF Core, and a Vue 3 single-page app (`web/`) built from shadcn-vue components in `web/src/components/ui`. Money is stored and sent in cents; the web formats it with `money()` from `web/src/lib/format.ts`.

Feature slices plug in without editing shared files. An API slice adds a class implementing `IEndpointModule` (see `api/Camp.Api/Infrastructure/Modules.cs`), discovered by assembly scan. New entities are mapped by an `IEntityTypeConfiguration<T>` in the slice folder, which `CampDbContext.OnModelCreating` picks up through `ApplyConfigurationsFromAssembly`. Demo rows come from an `ISeedModule` (runs after the core seed, ordered by `Order`, and must be idempotent). `IAuditLog.Record(...)` stages an audit row that the caller's `SaveChanges` commits.

Identity: guests sign in through WorkOS; on first sign-in a household is created by email. An endpoint that needs a signed-in guest uses `.RequireAuthorization(Policies.Family)` and injects `CurrentUser`, whose `HouseholdId` comes from the session cookie, never from the request. Anonymous endpoints (the secure link) use `.AllowAnonymous()` and identify the attendee only by the token.

Relevant existing rows: the Emerging Leaders Cohort (`Programs.Slug = 'emerging-leaders-cohort'`, `Type = Cohort`) has one session "September 2028" (Sept 13–15, 2028, $450) with one capacity pool "Attendees" (capacity 60). `IPaymentGateway` (`api/Camp.Api/Integrations/PaymentGateway.cs`) is the simulated Fiserv processor: `ChargeAsync(token, cents, idempotencyKey)`, `RefundAsync(processorRef, cents)`. The web obtains a token from `POST /api/fiserv-sandbox/tokenize`. `OutboxEvent` rows stand in for HubSpot email.

Web routes: a slice exports `routes` from `web/src/features/<slice>/routes.ts`; they become children of the guest layout. `meta.auth: 'family'` makes the router send a signed-out visitor through sign-in.

## Plan of Work

Milestone 1, API. In `api/Camp.Api/Features/Groups/` add `GroupEntities.cs` with `GroupRegistration` (leader household, session, name, status Draft or Confirmed, optional `PaymentOrder`), `GroupAttendee` (name, email, token hash, form status Complete or Incomplete, contact phone, answers JSON, submitted time, link-sent time, withdrawal status None/Requested/Approved/Declined with reason and times, and an active flag that turns false once a withdrawal is approved), and `GroupWaiverAcceptance` (attendee, waiver template, version snapshot, signer, time). Add their `IEntityTypeConfiguration` classes in the same file. Add `GroupService.cs` for roster validation, the claim-charge-finalize sequence, secure links, forms, and refunds, `GroupsEndpoints.cs` for HTTP, and `GroupsSeed.cs` (Order 100) for the cohort waivers and questions plus Pastor Dave's seeded group. Then add one migration named `Groups`.

Milestone 2, tests. `api/Camp.Api.Tests/GroupsTests.cs` covers: creating a draft with partial rows; paying charges price × attendees and raises the pool's `Reserved` by the attendee count; a declined card releases the seats and leaves the group in Draft; a group larger than the remaining seats is refused with nothing charged; another household can't see or act on the group (404) and a signed-out caller gets 401; staff get 403; the secure link shows only its own attendee, submits forms, and rejects a missing waiver; resend rotates the token so the old link returns 404; approving a withdrawal refunds $450 and releases one seat.

Milestone 3, web. In `web/src/features/groups/` add `routes.ts`, `types.ts`, `roster.ts` (paste/CSV parsing), `CardFields.vue` (simulated Fiserv fields), `MyGroups.vue` (`/groups`), `GroupRoster.vue` (R8, `/groups/new/:sessionId` and `/groups/:id/roster`), `GroupTracker.vue` (G2, `/groups/:id`), and `GroupLink.vue` (G1, `/g/:token`, no auth). Edit `ProgramDetail.vue` as recorded in the Decision Log.

Milestone 4, e2e. `e2e/groups.spec.ts`: Pastor Dave registers a group of three from the program page (one row without an email), pays, lands on the tracker, resends a link, the attendee completes forms in a separate signed-out browser context, the tracker shows the attendee Complete; the attendee requests withdrawal and Dave approves the refund. A second test checks the seeded group's canonical counts.

## Concrete Steps

From `/Users/ethanwoo/dev/camp-wt/groups`:

    scripts/dotnet.sh ef migrations add Groups --project api/Camp.Api --output-dir Data/Migrations
    ConnectionStrings__Default="Server=localhost,14333;Database=CampRegistration_Groups;User Id=sa;Password=Camp_Dev_Passw0rd!;TrustServerCertificate=True" WorkOS__RedirectUri=http://localhost:5183/api/auth/callback scripts/dotnet.sh run --project api/Camp.Api --urls http://localhost:5083
    API_URL=http://localhost:5083 npm --prefix web run dev -- --port 5183 --strictPort
    npm run test:api
    E2E_BASE_URL=http://localhost:5183 npx playwright test e2e/groups.spec.ts e2e/smoke.spec.ts

## Validation and Acceptance

- [x] `npm run lint` (pass)
- [x] `npm run typecheck` (pass)
- [x] `npm run check:api` (pass)
- [x] `npm run test:api` including `GroupsTests` (pass)
- [x] `E2E_BASE_URL=http://localhost:5183 npx playwright test e2e/groups.spec.ts e2e/smoke.spec.ts` (pass)
- [x] Desktop (1440×1000) and phone (390×844) screenshots of R8, G1, G2 compared with the concepts (pass)

## Idempotence and Recovery

The seed module checks for its own rows before inserting, so restarting the API is safe. To start over, drop the slice database: `docker exec camp-registration-prototype-db-1 /opt/mssql-tools18/bin/sqlcmd -C -S localhost -U sa -P 'Camp_Dev_Passw0rd!' -Q "DROP DATABASE [CampRegistration_Groups]"`, then restart the API. Checkout is idempotent on the client's key: a repeated submit returns the first outcome instead of charging twice.

## Artifacts and Notes

Self-review (seven passes) against the concepts at 1440×1000 and 390×844:
- Layout: the summary card's buttons collapsed to a sliver in the stacked column (`flex-1` in a column); fixed. The "existing groups" alert listed every group; now shows three and links to Your groups.
- Copy: the R8 info title truncated at phone width; shortened to "Attendees finish their own forms". G1 repeated the session name before the dates; removed.
- G2 vs concept: added a per-row "Resend link" button for incomplete attendees (the concept's primary row action); the bulk button reads "Resend to selected".
- Behavior: the refund confirm used `AlertDialogAction`, which closed before the handler ran (see Surprises); replaced.
- Status vocabulary: forms use Complete/Incomplete; groups use Draft/Confirmed; withdrawn attendees show Cancelled.
- Access: attendee pages show only their own record (asserted in e2e and GroupsTests); the tracker has no edit-forms control.
- Money: every amount goes through `money()`; totals come from the server once a draft is saved.

## Interfaces and Dependencies

HTTP surface (all JSON, money in cents):

    GET  /api/groups                                  family: my groups
    GET  /api/groups/start/{sessionId}                family: cohort session context for a new group
    POST /api/groups                                  family: create a Draft with a roster
    GET  /api/groups/{id}                             family (leader only): tracker data
    PUT  /api/groups/{id}/roster                      family (leader only, Draft only)
    POST /api/groups/{id}/checkout                    family: { idempotencyKey, cardToken }
    POST /api/groups/{id}/resend                      family: { attendeeIds } -> links sent
    PUT  /api/groups/{id}/attendees/{aid}/email       family: add or fix an email
    POST /api/groups/{id}/attendees/{aid}/withdrawal/approve|decline
    GET  /api/group-links/{token}                     anonymous: one attendee's forms
    POST /api/group-links/{token}/forms               anonymous: submit forms
    POST /api/group-links/{token}/withdrawal          anonymous: request withdrawal
